reticulate::repl_python()
reticulate::repl_python()
quarto render
git push
reticulate::repl_python()
reticulate::repl_python()
import os
print(os.getcwd())
import os
print(os.getcwd())
reticulate::repl_python()
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import LabelEncoder
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import LabelEncoder
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import LabelEncoder
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import LabelEncoder
# Your Python code goes here
# For example, you can use the imported libraries to perform tasks like loading data, splitting it, and encoding labels.
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import LabelEncoder
# Your Python code goes here
# For example, you can use the imported libraries to perform tasks like loading data, splitting it, and encoding labels.
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import LabelEncoder
